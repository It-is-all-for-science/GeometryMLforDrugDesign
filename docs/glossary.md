# Глоссарий терминов

## 🔤 Алфавитный указатель

[А](#а) | [Б](#б) | [В](#в) | [Г](#г) | [Д](#д) | [Е](#е) | [И](#и) | [К](#к) | [Л](#л) | [М](#м) | [Н](#н) | [О](#о) | [П](#п) | [Р](#р) | [С](#с) | [Т](#т) | [У](#у) | [Ф](#ф) | [Э](#э)

---

## А

**Активный центр (Active Site)** - область белка, где происходит связывание субстрата или лиганда. Имеет специфическую трехмерную структуру, комплементарную связывающейся молекуле.

**Aleatoric uncertainty** - неопределенность, связанная с шумом в данных; не может быть снижена увеличением размера датасета.

**Asphericity** - мера отклонения формы молекулы от сферической. Вычисляется на основе собственных значений тензора инерции.

**Acylindricity** - мера отклонения формы молекулы от цилиндрической. Характеризует анизотропию молекулы.

**Attention Pooling** - метод агрегации признаков, который использует механизм внимания для взвешенного усреднения. Позволяет модели автоматически определять важность различных элементов при агрегации.

**Attention weights** - веса внимания в нейронных сетях, показывающие, на какие части входных данных модель обращает наибольшее внимание при принятии решений.

**Attention entropy** - мера неопределенности в распределении весов внимания. Высокая энтропия указывает на то, что модель не может четко сфокусироваться на конкретных взаимодействиях.

**Аффинность связывания (Binding Affinity)** - мера силы взаимодействия между двумя молекулами (например, белком и лигандом). Обычно измеряется как константа диссоциации Kd или как свободная энергия связывания ΔG.

**Архитектура сети** - структурная организация нейронной сети, включающая количество слоев, их типы, размеры и способы соединения.

## Б

**Baseline модель** - простая модель, используемая как точка отсчета для сравнения с более сложными методами. Помогает оценить, действительно ли сложность оправдана улучшением результатов.

**Baseline сравнение** - методология оценки новых методов путем сравнения с простой, хорошо понятной моделью. Помогает определить, оправдана ли дополнительная сложность.

**Batch size** - количество образцов данных, обрабатываемых одновременно во время одной итерации обучения нейронной сети.

**Batch Normalization** - техника нормализации входов слоев нейронной сети, которая стабилизирует обучение и позволяет использовать более высокие learning rates. Нормализует активации по батчу.

**Белок-лигандный комплекс** - структура, образованная при связывании белка с малой молекулой (лигандом). Основа для понимания механизмов действия лекарств и разработки новых терапевтических агентов.

**Betti числа** - топологические инварианты, описывающие количество "дыр" различных размерностей в пространстве. B₀ - количество компонент связности, B₁ - количество циклов, B₂ - количество полостей.

## В

**Вращательная инвариантность** - свойство функции или модели оставаться неизменной при поворотах входных данных в пространстве.

## Г

**Геометрическое машинное обучение** - область ML, которая учитывает геометрическую структуру данных и использует симметрии для улучшения обучения и генерализации.

**Гиперпараметры** - параметры модели, которые задаются до начала обучения (например, learning rate, количество слоев). В отличие от весов, не обучаются автоматически.

**Гомология** - математическая концепция для изучения топологических свойств пространств. В контексте TDA используется для выявления "дыр" различных размерностей.

**Graph Attention Networks (GAT)** - тип графовых нейронных сетей, который использует механизм внимания для определения важности соседних узлов при агрегации информации.

**Graph Density** - отношение количества ребер в графе к максимально возможному количеству ребер. Характеризует связность молекулярного графа.

**Graph Isomorphism Networks (GIN)** - мощный тип GNN, который теоретически может различать любые графы, которые может различить тест Вейсфейлера-Лемана.

**GraphSAGE** - метод индуктивного обучения на графах, который изучает функцию агрегации для генерации представлений узлов путем сэмплирования и агрегации признаков из локального окружения.

**Группа симметрий** - математическая структура, описывающая набор трансформаций объекта, которые оставляют его неизменным.

## Д

**Диаграммы персистентности** - визуальное представление топологических признаков, показывающее, при каких масштабах появляются и исчезают топологические структуры.

**Докинг** - вычислительный метод предсказания того, как малая молекула (лиганд) будет связываться с белком-мишенью. Включает поиск оптимальной позы и оценку энергии связывания.

**Доверительный интервал** - диапазон значений, который с определенной вероятностью содержит истинное значение параметра.

## Е

**E(n) группа** - группа евклидовых трансформаций в n-мерном пространстве, включающая трансляции, вращения и отражения.

**Early stopping** - техника регуляризации, при которой обучение останавливается, когда производительность на валидационном наборе перестает улучшаться.

**Ensemble uncertainty** - оценка неопределенности через разброс предсказаний между несколькими моделями в ансамбле.

**Epistemic uncertainty** - неопределенность, связанная с недостатком знаний модели; может быть снижена добавлением данных.

**Extra Trees (Extremely Randomized Trees)** - ансамблевый метод, похожий на Random Forest, но с дополнительной рандомизацией при выборе разбиений в узлах деревьев.

**Эффективность модели (Model Efficiency)** - мера того, насколько хорошо модель использует свои ресурсы (параметры, время) для достижения качества. Высокоэффективная модель достигает хорошего качества с минимальными ресурсами.

**Эквивариантность** - свойство функции трансформироваться предсказуемым образом при трансформации входных данных.

## И

**Инвариантность** - свойство функции оставаться неизменной при определенных трансформациях входных данных.

**Inductive bias** - предположения, встроенные в алгоритм обучения, которые помогают ему обобщать на новые данные.

**Интерпретируемость (Interpretability)** - способность понимать и объяснять решения модели машинного обучения. В drug design критична для научной валидации и регуляторного одобрения.

**Индуцированное соответствие** - модель связывания, при которой белок и лиганд изменяют свои конформации для оптимизации взаимодействия. Более современная концепция по сравнению с моделью "замок-ключ".

## К

**Комплексы Вьеториса-Рипса** - способ построения симплициального комплекса из набора точек, соединяя точки, расстояние между которыми меньше заданного порога.

**Комплементарность форм** - принцип молекулярного распознавания, согласно которому биологические молекулы взаимодействуют наиболее эффективно при геометрическом соответствии их форм.

**Коррекция множественных сравнений** - статистические методы для контроля ошибок первого рода при проведении множественных тестов. Метод Бонферрони - один из самых консервативных подходов.

**Коэффициент эффективности топологии** - отношение улучшения качества к увеличению сложности при добавлении топологических признаков. Значение > 1 указывает на эффективность топологических признаков.

**Calibration** - свойство модели честно оценивать свою уверенность; хорошо откалиброванная модель правильно предсказывает вероятность своих ошибок.

**CNO Fraction** - доля атомов углерода, азота и кислорода в молекуле. Характеризует "органичность" соединения.

**Complexity Score** - комплексная метрика эффективности модели, учитывающая соотношение между качеством предсказаний и сложностью модели (количество параметров, время обучения).

**Confidence interval** - интервал значений, в котором с заданной вероятностью находится истинное значение предсказываемой величины.

## Л

**Learning rate** - скорость обучения, гиперпараметр, определяющий размер шагов при обновлении весов нейронной сети.

**Learning rate scheduling** - техника адаптивного изменения скорости обучения в процессе тренировки для улучшения сходимости.

**Lead optimization** - процесс улучшения свойств ведущих соединений (leads) для создания кандидатов в лекарства с оптимальной эффективностью, селективностью и безопасностью.

## М

**MAE (Mean Absolute Error)** - средняя абсолютная ошибка, метрика качества регрессии: среднее значение модулей разностей между предсказанными и истинными значениями.

**Message passing** - механизм в графовых нейронных сетях, при котором узлы обмениваются информацией со своими соседями.

**Molecular Descriptors** - численные характеристики молекул, описывающие их структурные, физико-химические или топологические свойства. Используются в QSAR моделировании.

**Molecular Weight** - сумма атомных масс всех атомов в молекуле. Важный дескриптор для ADMET свойств лекарств.

**Moments of Inertia** - тензор, описывающий распределение массы молекулы относительно осей вращения. Используется для характеристики формы молекулы.

**Молекулярные графы** - представление молекул в виде графов, где узлы соответствуют атомам, а ребра - химическим связям.

## Н

**Node embeddings** - векторные представления узлов графа, изучаемые нейронной сетью для кодирования информации об узлах и их окружении.

**Node Degree Distribution** - распределение степеней узлов в графе. В молекулярных графах отражает распределение валентностей атомов.

## О

**Overfitting** - переобучение, ситуация когда модель слишком хорошо запоминает обучающие данные и плохо обобщает на новые.

## П

**Персистентная гомология** - метод топологического анализа данных, изучающий, как топологические признаки изменяются при различных масштабах.

**Персистентные ландшафты** - функциональное представление диаграмм персистентности, позволяющее использовать их в качестве признаков для машинного обучения. Преобразуют диаграммы в векторы фиксированной длины.

**Permutation importance** - метод оценки важности признаков путем измерения снижения качества модели при случайном перемешивании значений каждого признака.

**Prior knowledge** - предварительные знания о задаче или данных, которые можно встроить в модель для улучшения обучения.

**p-value** - вероятность получить наблюдаемый результат (или более экстремальный) при условии, что нулевая гипотеза верна.

**P-value** - вероятность получить наблюдаемый или более экстремальный результат при условии, что нулевая гипотеза (отсутствие различий) верна. P-value < 0.05 обычно считается статистически значимым.

**Практическая значимость** - важность результата с точки зрения реального применения, в отличие от статистической значимости. Статистически значимое различие может быть практически незначимым.

## Р

**R² (коэффициент детерминации)** - метрика качества регрессии, показывающая долю дисперсии зависимой переменной, объясняемую моделью.

**RMSE (Root Mean Square Error)** - корень из среднего квадрата ошибок, метрика качества регрессии.

**Radius of Gyration** - мера компактности молекулы, определяемая как среднеквадратичное расстояние атомов от центра масс молекулы.

**Residual Connections (Skip Connections)** - архитектурная техника, при которой выход предыдущего слоя добавляется к выходу текущего слоя. Помогает обучению глубоких сетей, решая проблему исчезающих градиентов.

**Размер эффекта (Effect Size)** - стандартизованная мера величины различия между группами, независимая от размера выборки. Cohen's d - популярная мера размера эффекта.

**Размерность признаков** - количество числовых значений, используемых для представления объекта в машинном обучении. Высокая размерность может привести к проклятию размерности.

## С

**Симплициальные комплексы** - обобщение графов, включающее не только ребра (1-симплексы), но и треугольники (2-симплексы), тетраэдры (3-симплексы) и т.д.

**SO(3) группа** - группа вращений в трехмерном пространстве.

**SU(3) группа** - специальная унитарная группа размерности 3, используемая в квантовой механике.

**Статистическая значимость** - мера уверенности в том, что наблюдаемый эффект не является случайным.

**Статистическая значимость (Statistical Significance)** - мера того, насколько вероятно, что наблюдаемое различие между группами является результатом случайности, а не реального эффекта. Обычно оценивается через p-value.

**Set2Set Pooling** - продвинутый метод агрегации для множеств переменного размера, который использует LSTM для итеративного "чтения" элементов множества и создания фиксированного представления.

**SiLU (Swish) Activation** - функция активации f(x) = x * sigmoid(x), которая часто показывает лучшие результаты чем ReLU в глубоких сетях. Гладкая и дифференцируемая везде.

**SAR (Structure-Activity Relationship)** - взаимосвязь между химической структурой молекулы и ее биологической активностью. Основа для рационального дизайна лекарств.

**Стерическая комплементарность** - геометрическое соответствие форм белка и лиганда, критичное для эффективного связывания. Включает объемное соответствие и отсутствие стерических столкновений.

## Т

**Табличные методы** - традиционные алгоритмы машинного обучения (Random Forest, SVM, Gradient Boosting и др.), работающие со структурированными данными в виде таблиц признаков.

**Топологические дескрипторы** - числовые характеристики, описывающие топологические свойства молекулярных структур (циклы, полости, связность). Включают числа Бетти, статистики персистентности, персистентные ландшафты.

**Топологическая комплементарность** - мера соответствия топологических свойств двух молекулярных структур, важная для предсказания взаимодействий белок-лиганд. Учитывает совместимость форм активного центра и лиганда.

**Трансляционная инвариантность** - свойство функции оставаться неизменной при сдвигах (перемещениях) входных данных в пространстве.

**t-test** - статистический тест для сравнения средних значений двух групп.

**T-test (Т-тест)** - статистический тест для сравнения средних значений двух групп. Парный t-test используется для сравнения связанных измерений (например, ошибок одних и тех же моделей на одних данных).

## У

**Uncertainty quantification** - методы оценки неопределенности в предсказаниях машинного обучения, критически важные для принятия решений в условиях риска.

## Ф

**Фармакофор (Pharmacophore)** - набор структурных признаков молекулы, необходимых для биологической активности. Включает расположение донорных/акцепторных групп, гидрофобных областей и ароматических колец.

**Feature engineering** - процесс создания новых признаков из исходных данных для улучшения производительности модели.

**Feature Engineering** - процесс создания новых признаков из исходных данных с использованием доменных знаний для улучшения производительности модели.

**Feature Augmentation** - процесс добавления дополнительных признаков к исходному представлению данных для улучшения качества модели. В контексте молекулярного ML - добавление топологических признаков к геометрическим.

**Failure mode analysis** - систематический анализ случаев, когда модель дает неточные предсказания, для понимания ограничений и улучшения производительности.

## Э

**Эквивариантность** - свойство функции трансформироваться предсказуемым образом при трансформации входных данных.

---

## 📝 Примечания

- Этот глоссарий будет пополняться новыми терминами по мере прохождения проекта
- Для каждого термина приводится определение в контексте нашего исследования
- Сложные математические концепции объясняются простым языком с примерами

---
